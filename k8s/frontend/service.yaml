apiVersion: v1
kind: Service
metadata:
  name: stafford-chess-frontend-service
  namespace: frontend
spec:
  selector:
    app: stafford-chess-frontend
  ports:
  - port: 80
    targetPort: 80
  type: ClusterIP
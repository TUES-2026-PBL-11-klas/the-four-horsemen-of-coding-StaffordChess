# /tmp/observability-test/promtail.yaml
server:
  http_listen_port: 9080
  grpc_listen_port: 0

positions:
  filename: /tmp/positions.yaml

clients:
  - url: http://loki:3100/loki/api/v1/push

scrape_configs:
  # Watch a local test log file
  - job_name: test-logs
    static_configs:
      - targets:
          - localhost
        labels:
          job: test
          app: fastapi-backend
          __path__: /test-logs/*.log
    pipeline_stages:
      - regex:
          expression: '^(?P<timestamp>\d{4}-\d{2}-\d{2} \d{2}:\d{2}:\d{2},\d+)\s+(?P<level>\w+)\s+.*?- (?P<message>.+)$'
      - labels:
          level: